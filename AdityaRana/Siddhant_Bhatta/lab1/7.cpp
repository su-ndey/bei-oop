#include <iostream>
using namespace std;
int main()
{
    int a, l, b;
    cout << "Enter the length and breadth:" << endl;
    cin >> l >> b;
    cout << "Area of rectangle:" << (l * b);
    return 0;
}